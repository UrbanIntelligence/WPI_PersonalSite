# Live site backup — 2026-08-19

Snapshot of the files on `public_html` at `https://users.wpi.edu/~yli15/` **immediately before** the redesigned site (this repo's root files) was deployed over them.

Scope: only the 10 HTML pages and the `img/` files that the redesign actually overwrites. WPI's `public_html` also contains a large amount of unrelated content (course materials, old project pages, PDF archives, etc.) that this redesign never touches, so it isn't duplicated here — nothing there is at risk of being lost.

To roll back to this exact pre-redesign version, copy these files back into `public_html`.
